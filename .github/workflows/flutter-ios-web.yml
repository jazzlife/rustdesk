name: Flutter iOS & Web Only Build

on:
  workflow_dispatch:

jobs:
  run-flutter-ios-build:
    uses: ./.github/workflows/flutter-build-ios-web.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: "custom"
