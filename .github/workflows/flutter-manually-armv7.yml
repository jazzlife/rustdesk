name: Flutter Manually armv7 Build

on:
  workflow_dispatch:

jobs:
  run-flutter-manually-armv7-build:
    uses: ./.github/workflows/flutter-armv7-android.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: "1.3.3"
